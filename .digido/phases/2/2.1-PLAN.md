# Phase 2.1: מסך Streamlit + העלאת קבצים

**מטרה:** לבנות את מסך ה-Upload — המנהל מעלה 3 קבצים ו-`app.py` מעבד אותם דרך `data_loader`.

**קבצים מעורבים:**
- `app.py` — הרחבה מ-Phase 1.1

**Tasks:**
- [ ] הוסף ל-`app.py` Sidebar עם 3 `st.file_uploader` (הזמנות, לקוחות, מוצרים) — תמיכה ב-CSV ו-XLSX
- [ ] חבר את הקבצים המועלים לפונקציות `load_and_validate` ו-`merge_data` מ-`data_loader.py`
- [ ] הצג `st.spinner("מעבד נתונים...")` בזמן מיזוג — ואחרי הצלחה: `st.success("הנתונים נטענו בהצלחה ✅")`

**הערות:**
- אם קובץ לא הועלה — אל תריץ עיבוד. הצג הודעה: `"⬆️ אנא העלה את שלושת הקבצים כדי להציג את הדשבורד"`
- `session_state` שומר את ה-`merged_df` כדי שה-Phases הבאים ישתמשו בו

**Success Criteria:**
- העלאת 3 קבצי `*_deme.csv` מציגה `st.success` ללא קריסה
- חסרון קובץ מסיים בהודעה ידידותית ולא בשגיאת Python
- `st.session_state["merged_df"]` קיים ומכיל נתונים אחרי העלאה מוצלחת
